# Progress

## 2026-08-11 提交历史清洗

- Branch: `main`
- Changes: Rewrote the 14-commit `main` history to remove the `Co-Authored-By` trailers that named Claude or Anthropic from three commits. No file-tree content changed, and non-Claude co-author trailers were left intact.
- Verification: The rewritten history contains 0 matching Claude/Anthropic co-author trailers; `git diff --exit-code origin/main^{tree} main^{tree}` passed before this log entry was added, confirming the rewrite itself preserved the repository tree. The updated history was pushed to `origin/main` with `--force-with-lease`.
- Remaining issues: Existing clones of the old `origin/main` history must fetch and realign their local branch to the rewritten history. Upstream history was intentionally not modified.

## 2026-08-07

- Branch: `main`
- Changes: Generated the complete `submissions/lumixraku/rail-life-rings/` professional design package, including proposal narrative, structured geometry and metrics, evidence matrices, five figures, offline HTML, and A3/A0 PDF drawings. Corrected geometry schema values, evidence references, visual markers, and land-use topology.
- Verification: `.venv/bin/python scripts/self_check_submission.py submissions/lumixraku/rail-life-rings --pr-author lumixraku` returned `PASS`; deterministic, spatial, visual packaging, and professional evidence reviews all passed.
- Remaining issues: Official site and key-area boundaries are not present in the repository. The package explicitly labels the three key-area geometries as provisional; spatial review reports only the expected non-blocking `KEY_AREA_PROVISIONAL` notices.

## 2026-08-08 图纸重制：真 A0/A3 矢量与逐站设计

- Branch: `main`
- Changes: Rebuilt both PDF drawings from scratch. The previous files were raster-only (15 objects, no embedded fonts, JPEG-only) and both measured A2 despite being labelled A0 and A3; 4 of their 9 embedded images were byte-identical across the two documents, and the 5/10/15-minute ring labels shared one baseline, which produced the reported text overlap. The new `a0-boards.pdf` (4 plates, true 841x1189mm) and `a3-booklet.pdf` (11 pages, true 297x420mm) are pure vector with embedded CJK fonts and a selectable, searchable text layer, and share no duplicated content: A0 carries corridor structure, severance diagnosis, per-station design and key-area detail; A3 is a reading document with one page per station.
- Station specificity: the package previously named no real station, because `assumptions.json` A-TOD-001 placed the three TOD cores at the geometric centroids of the key-area polygons. Every station inside the announced boundary is now identified by point-in-polygon test against `brief/site-package/geometry/provisional_boundaries.geojson`: 7 stations fall inside the 11.4km² overall design area (学知园、六道口、学院桥、西土城、蓟门桥、北京北、西直门) and receive per-station design depth (role, severance problem, 5-minute core programme, 10-minute ring programme, near-term moves, survey prerequisites); 14 stations fall inside the 43.6km² coordinated research area and receive corridor and interchange roles only. Scope follows the announced boundary per owner decision of 2026-08-08, so 上地、清河、西二旗 (2.5km, 3.3km and 4.6km beyond the nearest key area, all north of the 北五环) are excluded from per-station drawings and the exclusion is stated on plate L-03.
- Method: walk-time rings are no longer concentric circles around polygon centroids. Radius is `minutes x 75 m/min / 1.35 detour factor` from real station coordinates (278m, 556m, 833m), and motorway/trunk severance lines are overlaid so nominally-reachable-but-actually-detoured areas are legible. The base map uses 2343 classified OSM road ways, 244 rail ways, and green and water layers, replacing the previous 4 schematic lines.
- Verification: `.venv/bin/python scripts/self_check_submission.py submissions/lumixraku/rail-life-rings --pr-author lumixraku` returned `PASS`, and `validate_local_submission.py` returned `PASS`, with only the pre-existing provisional-boundary warnings. A span-level layout audit over all 15 pages reports 0 text runs outside the printable margin and 0 overlapping text boxes. `manifest.json` sha256 entries for both drawings were recalculated.
- Remaining issues: `PROV-KEY-003` is labelled 大钟寺 but its polygon lies at lat 39.944-39.950 while 大钟寺 station is at 39.965, a discrepancy of roughly 1.7km; the deviation is drawn and dimensioned on plate L-04 and A3 page 11 but the repository geometry was left unmodified pending official key-area boundaries. A `geometry/stations.geojson` layer could not be added because `ALLOWED_GEOMETRY_FILES` in `scripts/validate_submission.py` is a closed nine-file allowlist. Station coordinates remain provisional OSM data and require surveyed station entrances. Official redlines, regulatory controls, road redlines, ownership, municipal and heritage constraints remain unpublished.

## 2026-08-08 强化轨道 TOD 内容：正文与首页

- Branch: `main`
- Changes: The owner asked for the whole submission to emphasise rail TOD, reporting that the planning text never mentioned it. Verified: `TOD` did appear 12 times in `proposal.md` including the title, so the literal claim was inaccurate, but the substance of the complaint was correct and more serious than stated — TOD was a label with no method behind it. Station-area TOD vocabulary was entirely absent (`站城一体`, `站域`, `站点分类`, `密度梯度`, `用地混合`, `出入口`, `P+R`, `站前` all 0 occurrences), and none of the seven per-station-detailed stations were named anywhere in the body text; only `大钟寺` appeared, as an area name rather than a station. The drawings and figures had been rebuilt around real stations while the proposal text still organised itself around key-area polygons and "three conceptual TOD nodes". Fixed inside the existing section structure without adding chapters: the three cores are now anchored to 学知园 (line 27), 学院桥 (line 27) and 北京北 (suburban rail S5); the three-level framework states the full 21-station derivation with all 7 design-area and 14 corridor stations named with their lines; a station classification table sorts the 7 into city-level interchange anchors, station-city integrated cores and community feeder stations, each with its own design emphasis; station-area extent is defined from station entrances by `minutes x 75 m/min / 1.35`, matching the drawings; new subsections cover station-city ground-floor continuity, transfer and feeder organisation, bicycle and P+R policy, and station forecourts; 蓟门桥 received its own subsection because it belongs to no key area and had otherwise been listed without a programme; and the 大钟寺 section now states plainly that 北京北 is the station inside `PROV-KEY-003` and carries the 1.7km discrepancy note. `README.md` received the matching treatment: real station names in the three-core list, the station classification table, station-area radii, transfer and density-gradient principles, and the 14 corridor stations.
- Density: FAR and building height remain `unknown`, so the station-area density gradient is stated only as relative relationships and control principles. No numeric intensity was invented; this is recorded as a data gap, not an omission.
- Verification: `.venv/bin/python scripts/self_check_submission.py submissions/lumixraku/rail-life-rings --pr-author lumixraku` returned `PASS` with `can_enter_formal_review=true`, and `validate_local_submission.py --pr-author lumixraku` returned `PASS`, both with only the pre-existing provisional-boundary and maintainer-review warnings. An intermediate self-check correctly failed on a `proposal.md` sha256 mismatch, which confirmed the manifest check is live rather than vacuous; `report/proposal.html` was regenerated with `scripts/render_proposal_html.py` so the offline reading copy carries the new text, and all 30 manifest hashes were resynced and then verified against the files on disk.
- Remaining issues: The `README.md` edit re-diverges the repository homepage from upstream. That is what the owner asked for on their own fork, but PR template rule 52 forbids modifying `README.md` in a submission pull request, so an upstream submission would require reverting it first. The drawing and figure generators plus matplotlib are still absent from the repository. The `PROV-KEY-003` discrepancy, the `geometry/stations.geojson` allowlist block, and the provisional OSM station coordinates are unchanged.

## 2026-08-08 图纸重制补齐：README 五张图

- Branch: `main`
- Changes: Regenerated all five proposal figures (`site-overview.png`, `land-use-structure.png`, `key-areas.png`, `mobility-bluegreen.png`, `metrics-evidence.png`). The 2026-08-08 rebuild replaced the two PDFs but not these PNGs, which dated from 2026-08-07 and were built on the same method the rebuild had removed: TOD cores at key-area polygon centroids, concentric-circle walk rings, no station named, and the 5/10/15-minute labels written at a single shared baseline, which left visibly garbled overlapping text in `site-overview.png` and `key-areas.png`. `key-areas.png` also drew the same identical circle diagram three times for three different key areas. The README therefore still displayed the superseded method after the drawings were fixed. All five are now generated from the same data path as the plates: real OSM station coordinates, detour-adjusted radii, motorway and trunk severance overlay, and the 2343-way classified road base. Figure 1 names all 21 stations, figure 2 adds a per-station 5/10-minute programme table for the 7 design-area stations, figure 3 gives each key area its own map and its own station programme, figure 4 lists all 14 corridor interchange roles, and figure 5 reports the metric recalculation, station coverage and outstanding data gaps. Filenames are unchanged because `REQUIRED_PROPOSAL_IMAGE_PATHS` in `scripts/validate_submission.py` fixes them.
- Layout: the corridor is a 5.1km x 9.9km strip and the design area a 1.4km x 9.7km strip, so a landscape map axes had to pad sideways to keep scale isotropic and left the sheet half empty. Each map is now a portrait panel and the freed width carries station content, which is also what the original figures were criticised for lacking.
- Verification: `.venv/bin/python scripts/self_check_submission.py submissions/lumixraku/rail-life-rings --pr-author lumixraku` returned `PASS` with `can_enter_formal_review=true`, and `validate_local_submission.py --pr-author lumixraku` returned `PASS`, both with only the pre-existing provisional-boundary and maintainer-review warnings. Every figure was inspected as a rendered image, and four layout defects found that way were fixed before installing: unreadably faint walk rings, a panel title colliding with the sheet header, a legend overlapping itself and the scale bar, and empty sheet areas. All five `manifest.json` sha256 entries were recalculated and all 30 manifest hashes were then verified against the files on disk. Each PNG is between 301KB and 826KB, within the 5MB `MAX_ASSET_BYTES` cap. `report/proposal.html` references the figures by relative path, so it picks up the new files without regeneration.
- Remaining issues: The figure generator (`make_figures.py`) shares the workspace-only status of the plate generators, so the figures cannot be reproduced from a clone; matplotlib is still not in `requirements-review.txt`. The three earlier open items are unchanged: the `PROV-KEY-003` 1.7km polygon discrepancy, the `geometry/stations.geojson` allowlist block, and station coordinates remaining provisional OSM data pending surveyed entrances.

## 2026-08-09 图纸生成器入库：tools/ 可复现

- Branch: `main`
- Changes: Committed the drawing generators to a repository-root `tools/` directory, closing the reproducibility gap that had been carried as a known issue since 2026-08-08. Until now both PDFs and all five figures existed only as output: the scripts that produced them lived in the agent workspace, so a clone could not regenerate or amend a single sheet, and nobody could check whether a drawing actually followed the method the text claimed. `tools/` holds `basemap.py` (EPSG:32650 projection and the `walk_ring()` radius rule), `draw.py` (the shared page frame, label placer, CJK wrapper, legend and scale bar used by all three generators), `make_a0.py`, `make_a3.py`, `make_figures.py`, `station_program.py`, `audit.py`, the two Overpass fetch scripts, and `data/` with the OSM exports plus `stations.geojson`. Two absolute `~/others/haidian` paths in `basemap.py` and `make_figures.py` were made repo-relative, and the fetch scripts and `audit.py` now resolve their own directory rather than the caller's cwd, so the scripts work from a clone at any location without editing. `tools/README.md` documents the run sequence, the file map, the OSM provenance and ODbL attribution, the STHeiti two-file font requirement, and the two traps in `draw.py` that caused the worst defects — the `SCALE` factor that any hand-computed text metric must also apply, and the `set_aspect("equal")` behaviour that `fit_extent` exists to work around. `tools/requirements.txt` pins matplotlib 3.11.1, pyproj 3.7.2 and shapely 2.1.2, because a different matplotlib minor version changes text metrics and therefore changes where the label solver puts labels. `tools/out/` is gitignored so generated output does not enter the repository.
- Verification: reproduction was checked by regenerating from the committed `tools/` copy and comparing against the files already installed in the submission package, not merely by confirming the scripts run. Both PDFs are identical page by page in both the text layer and the vector drawing primitives across all 9 A0 plates and all 11 A3 pages; all five PNGs are identical pixel for pixel. The PDF `sha256` values still differ, and only because matplotlib writes a creation timestamp into the document metadata — there is no content difference. `scripts/self_check_submission.py --pr-author lumixraku` returned `PASS` with `can_enter_formal_review=YES` and `validate_local_submission.py --pr-author lumixraku` returned `PASS` with `tools/` present, both with only the pre-existing maintainer-review and provisional-boundary warnings, confirming the new directory does not disturb package validation.
- Remaining issues: `tools/` cannot go upstream. `scripts/validate_submission.py` restricts participant PRs to `submissions/<github-login>/`, and the submission directory is itself a closed allowlist admitting only `.pdf` drawings, image assets and named json/geojson files, so a `.py` file has nowhere to live inside it. `tools/` therefore joins `README.md` and `progress.md` as fork-only content that an upstream submission PR must exclude; this is stated at the top of `tools/README.md` so the constraint is not rediscovered later. The generators assume macOS system STHeiti font files and need a substitute CJK font elsewhere. `matplotlib` remains absent from `requirements-review.txt`, which is a maintainer-controlled file. The `PROV-KEY-003` 大钟寺 polygon discrepancy, the `ALLOWED_GEOMETRY_FILES` allowlist block on `geometry/stations.geojson`, and the provisional OSM station coordinates pending surveyed entrances are unchanged.

## 2026-08-09 README 骨架重组：六章分层

- Branch: `main`
- Changes: The owner's complaint that the README structure was messy was only half addressed by the earlier content pass, which fixed what the sections said without touching how they were ordered. The page was 14 flat `##` sections in roughly the order they had been written, and the ordering actively contradicted the submission's own method: the rail-TOD rules that the per-station sections apply — transfer hierarchy, station-city ground-floor continuity, the density gradient — sat in a `交通与公共空间` section placed *after* the seven station sections that depend on them, so a reader met the conclusions before the method. `站点口径与分类` and the walk-radius formula were also split apart even though the formula is what makes the tiering measurable, and `服务谁` came ten sections after the concept it justifies. Reorganised into six numbered parts with a table of contents: (一) 服务谁与空间结构, (二) 车站口径与 TOD 方法, (三) 逐站设计的 7 座车站, (四) 廊道接驳与重点区原型, (五) 公共空间 AI 与分期, (六) 指标 边界与完整成果. Part 二 now gathers the whole method — the 21-station point-in-polygon tiering, the station classification table, the entrance-origin radius formula, the severance red-line caveat, transfer hierarchy, station-city ground-floor continuity and the density gradient — ahead of the stations that apply it, and states that dependency in one line. Part 三 cross-references the plate and page numbers (`L-02`..`L-08`, pages 03-09) so a reader can move between the README and the drawings. The one-line proposal moved above the first figure so the page opens on the claim rather than on an image.
- Content: this was a reordering, not a rewrite. A fragment-level diff against the previous README reports every apparently-dropped fragment reappearing in the new text; the 11 flagged as lost are adjacency artefacts of the changed headings, not deletions. New prose is limited to the table of contents, the six part headings, the sentence stating that part 二 is the premise of part 三, and the plate/page cross-reference. No metric, station, programme item or caveat was added, removed or reworded.
- Verification: `.venv/bin/python scripts/self_check_submission.py submissions/lumixraku/rail-life-rings --pr-author lumixraku` returned `PASS` with `can_enter_formal_review=YES`, and `validate_local_submission.py --pr-author lumixraku` returned `PASS`, both with only the pre-existing maintainer-review and provisional-boundary warnings. All 7 new in-page anchor links resolve against the generated heading slugs, all 5 figure paths exist, and all 6 relative artifact links (`proposal.md`, `visual/index.html`, both PDFs, `geometry/`, `report/copyright_statement.md`) resolve on disk.
- Remaining issues: `README.md` is outside the submission package, so neither validator scores it — the link and anchor checks above are the only automated coverage it has. The divergence from upstream `README.md` also still means an upstream submission PR would have to revert this file first, per PR template rule 52. The generators remaining workspace-only, the `PROV-KEY-003` discrepancy, the `ALLOWED_GEOMETRY_FILES` allowlist block and the provisional OSM station coordinates are all unchanged.

## 2026-08-09 A0 逐站重制与 A3 图册逐页校核

- Branch: `main`
- Changes (A0, committed as `d4e6535a`; this entry backfills the progress log the commit omitted): The A0 set was 4 plates with all 7 design-area stations packed into a 3x3 grid, so each station received one postcard-sized cell — a station index rather than per-station design. It is now 9 plates: `L-01` corridor structure and the 21-station tiering, `L-02`..`L-08` one full plate per design-area station, `L-09` key-area prototypes plus the 大钟寺 polygon check. The severance diagnosis that had been its own plate is folded into each station's plate, because severance is per-station: the barrier at 学知园 (京藏高速 + 北五环) is not the barrier at 西直门. Severance is now reported in two tiers — grade-separated expressways (OSM `motorway`/`trunk`) as hard breaks, wide at-grade arterials (`primary`) as signal-delay crossings. The single tier printed `0 段` at 六道口 even though that station's own diagnosis names 学院路, which OSM classes as `primary`; one tier on the plates and another in the text would have been two different diagnoses of the same station inside one submission.
- Changes (A3, this pass): Rewrote the per-station page on the same two-tier severance and the same `fit_extent` scaling as A0, then inspected all 11 pages as rendered images. Two defects were found that way and fixed. Pages 01 and 10 ended their text at mid-sheet with the bottom half blank, because both were hand-positioned runs with no floor: `fit_blocks` in `draw.py` could only shrink type to fit, never grow it to fill, so a short stack kept its nominal size and left the sheet looking unfinished. `fit_blocks` gained a `grow_max` parameter and both pages were converted to it. Page 10's title also read `统筹研究范围内站点 · 廊道与接驳层级` with the qualifier competing with the subtitle; it is now `廊道与接驳层级的 14 座站点` with the scope statement moved into the subtitle.
- Layout and typography fixes in the shared `draw.py`, so they apply to A0, A3 and the README figures alike: the `LabelPlacer` collision box was measured without `SCALE` while `font()` rendered with it, so on an A0 sheet (`SCALE`≈4) every reserved box was a quarter of the real glyph size and the overlap test almost never fired — this was the root cause of the overlapping labels, not the multi-line box estimate I first suspected. Boxes are now measured at `size * SCALE` and per line. The larger boxes then dropped 10 labels, so the candidate ring went from 8 anchors to 24 (three distance multipliers) with a two-stage `force` fallback for the subject of a sheet, where a missing station name is worse than a nudged one. `wrap_cjk` was rewritten to tokenise before wrapping, because it had been splitting latin runs and printing `27` + `8m` and `Op` + `enStreetMap`; it now binds `/ . -` so `m/min`, `1.35` and `5/10/15` stay whole, and it applies CJK 禁则 so a line cannot open with `，` or `。`. Legends gained a white backing panel plus a placer reservation (road strokes had been running through legend text, and station names had been landing underneath), and their width is measured from the longest entry instead of hand-set. Ring labels moved from exactly `r` to `r * 1.06` so the ring stroke no longer runs through its own label, and the SITE boundary is drawn dashed below the station dots because a solid line had hidden 西直门's dot.
- Verification: `.venv/bin/python scripts/self_check_submission.py submissions/lumixraku/rail-life-rings --pr-author lumixraku` returned `PASS` with `can_enter_formal_review=YES`; `validate_local_submission.py --pr-author lumixraku` returned `PASS` over 30 changed files. Both report only the pre-existing maintainer-review and provisional-boundary warnings and the three non-blocking `KEY_AREA_PROVISIONAL` notices. All 9 A0 plates and all 11 A3 pages were inspected page by page as rendered images at dpi 284. A monkeypatched `LabelPlacer.place` counter reports 0 dropped labels on both documents, and a block-level bounds check reports 0 text blocks outside the sheet frame at an 8pt margin. Page geometry is exact: A0 `841 x 1189` mm, A3 `297 x 420` mm. Embedded fonts are `STHeitiTC-Light` and `STHeitiTC-Medium` subsets only, with no DejaVu fallback, over 10827 characters (7420 CJK) on A0 and 6807 (4901 CJK) on A3, so the text layer stays selectable and searchable. The `manifest.json` rehash touched exactly the regenerated files, which confirms nothing else drifted.
- Remaining issues: The A0 commit `d4e6535a` did not update this progress log; that omission is corrected here rather than by rewriting the commit. The README skeleton is still organised as one long homepage rather than around the per-station structure the drawings now use — the owner's complaint about README structure was only half addressed by the earlier per-station content pass and remains open. The generators (`draw.py`, `make_a0.py`, `make_a3.py`, `make_figures.py`, `basemap.py`, `station_program.py`) still live only in the agent workspace, so neither PDF can be reproduced from a clone and matplotlib is still absent from `requirements-review.txt`. The `README.md` divergence from upstream, the `PROV-KEY-003` 大钟寺 polygon discrepancy (about 1.7km, drawn and dimensioned but not corrected in repository geometry), the closed `ALLOWED_GEOMETRY_FILES` allowlist blocking `geometry/stations.geojson`, and the provisional OSM station coordinates pending surveyed entrances are all unchanged.

## 2026-08-09 7 站三维鸟瞰，并修掉把河画成湖的底图 bug

- Branch: `main`
- Changes (why not Amap, stated up front because the owner asked for Amap 3D specifically): the key supplied for this project is a **Web-service** key. It is the right key for the walking-route REST API and did produce the 784 measured routes, but Amap gates platforms separately and its 3D buildings are drawn only by the **Web (JS API)** platform; calling `webapi.amap.com/v4/map/styles` with this key returns `USERKEY_PLAT_NOMATCH / 10009`, and the same key succeeds against `restapi.amap.com` in the same session, which isolates the cause to platform type rather than a bad or expired key. Amap also publishes no public street-view API, so a "3D 街景" sheet cannot be produced from Amap at all. Rather than block on a new key, the massing was rebuilt from OpenStreetMap building footprints, which needs no key, shares the projection and palette of every other sheet, reproduces from a clone, and is redistributable under ODbL. A Chrome-rendered Amap comparison remains possible later if a Web (JS API) key is supplied.
- Changes (the sheets): `tools/make_3d.py` renders one bird's-eye PNG per design-area station into `assets/figures/3d/`, with a hand-written perspective camera (station-centred, due south, 31° elevation, 46° FOV, north up, no vertical exaggeration) rather than matplotlib's `mplot3d`, because the ground layers, the extruded walls and the labels all need to sort in one painter's order. Footprints are extruded with back-face culling and per-face Lambert shading; the measured isochrone lies on the ground plane with the circle it replaced dashed beside it, and the ring outline is re-traced above the massing at reduced opacity because at this pitch the buildings otherwise hide the boundary that is the point of the sheet. The camera is identical on all seven, so they can be compared side by side. `tools/fetch_buildings.py` pulls the footprints (3686 buildings within 1200 m of each of the 7 stations) via Overpass with three mirrors in rotation.
- Changes (height is declared, not invented): only **387 of 3686 buildings (10.5%)** carry `height` or `building:levels`. Those are drawn in a darker tone with a roof outline; the remainder are estimated from building type by a table in `make_3d.py` and drawn paler with no roof outline, and every sheet prints both counts in its legend. `sources.json` gained `OSM-BUILDINGS` stating that footprints are as mapped while heights are not measured, `report/copyright_statement.md` records both the OSM attribution and the Amap key refusal, and `proposal.md` says in the body that massing is spatial context for reading the isochrone shape, not a height survey and not an existing-condition metric.
- Changes (a real data bug found by rendering, not by reading code): the 西直门 sheet came out with half the frame washed pale blue. `load_polys()` in `basemap.py` closed every way in `osm_water.json` into a polygon, including `waterway=river` centrelines — and 护城河 is tagged 已盖板 (culverted), so closing its centreline produced a **2089 ha** slab of water lying across the station it runs past. `load_polys()` now skips anything carrying a `waterway` tag and requires a closed ring (first node equal to last), leaving rivers to `load_water_lines()` where they belong. Largest water polygon dropped from 2089 ha to 36 ha, total from ~2249 ha to 160 ha. **This bug was in the shipped figures and PDFs**, not only in the new sheets, so `site-overview.png`, `land-use-structure.png`, `key-areas.png`, `mobility-bluegreen.png` and both PDFs were regenerated; `metrics-evidence.png` is unaffected and byte-identical.
- Verification: `scripts/validate_submission.py --pr-author lumixraku` returned `PASS` over 19 changed files, and `scripts/review_submission.py` reports pre-submit self-check `PASS`, professional evidence review `PASS`, `can enter formal professional review: YES` — both with only the pre-existing maintainer-review and provisional-boundary warnings. `tools/audit.py` reports 0 off-frame text and 0 label overlaps across all 9 A0 plates and all 11 A3 pages after regeneration. All 7 new sheets were inspected as rendered images, which is how three defects were found and fixed: the ring labels stacked on top of each other and on the station name at the severed stations (5- and 10-minute reaches there are only tens of metres apart, so placement is now chosen in screen space against already-placed boxes); the painter's z-order ran `20 + i` over 500+ buildings and so painted past the annotation layer, burying the camera note (the band is now normalised into a bounded range); and the line label printed a bare `27`, which in a sheet corner reads as a page number. The camera basis was checked numerically for orthonormality and for north-up / east-right / correct depth ordering before any sheet was trusted. `manifest.json` registers the 7 new figures and every `sha256` was recomputed; the script also asserts that no file on disk is missing from the manifest, and the manifest's own entry deliberately carries no hash.
- Remaining issues: heights remain 89.5% estimated — a real massing study needs a height survey or a licensed 3D dataset, and no sheet should be read as one. There is still no Amap-rendered comparison and no street view, pending a Web (JS API) key for the former; the latter is not obtainable from Amap at any key type. The views show Amap's walking network and OSM massing, so they still do not account for crossing signal delay, walled breaks, accessibility or night conditions, and station origins remain OSM provisional points rather than surveyed entrances. The 14 corridor stations have neither measured isochrones nor 3D sheets. `tools/` remains fork-only and `matplotlib` is still absent from the maintainer-controlled `requirements-review.txt`; the `PROV-KEY-003` polygon discrepancy, the `ALLOWED_GEOMETRY_FILES` block on `geometry/stations.geojson`, and the `README.md` divergence from upstream are unchanged.

## 2026-08-09 站域改为实测等时圈：同心圆下线

- Branch: `main`
- Changes: Every station-area ring in this submission was a circle of `radius = minutes * 75 / 1.35`, and `walk_ring()` in `basemap.py` carried a docstring claiming it was "not a naive circle" while returning `station["pt"].buffer(r)`. Measurement retired both the constant and the shape. Using the Amap walking-route API, 16 bearings per station at 22.5 degrees, 7 straight-line probe distances per bearing, 784 real walking routes across the 7 design-area stations, interpolating on each bearing the straight-line distance at which route length reaches the walking budget: the drawn rings were wrong two ways. The measured median detour is **1.75, not 1.35**, so every ring was drawn too large — the seven 15-minute rings claimed **1526 ha against 961 ha actually reachable, overstating by 37%**. More important, **reachable extent is not a circle**: at 学知园 the 5-minute reach is 284 m on the clear bearing and **38 m across 京藏高速**, a factor of 7.5; at 蓟门桥 it ranges 40–260 m. A circle simultaneously overstates severed bearings and understates open ones, which erases this proposal's own core argument that access is nominal and the walk is a detour. Area reduction against the drawn circles at 5/10/15 min: 学知园 −77/−72/−64%, 蓟门桥 −69/−58/−49%, 西直门 −55/−31/−37%, 学院桥 −54/−31/−17%, 六道口 −51/−51/−38%, 北京北 −41/−35/−34%, 西土城 −35/−22/−20%. The station measuring worst is exactly the one the drawings already annotated as doubly severed by 京藏高速 and 北五环, so measurement upgraded that annotation from assertion to quantified fact. `walk_ring()` now returns the measured polygon and its median reach, `reach_at()` returns the boundary distance along a specific bearing so a ring label lands on the boundary rather than inside or outside it, and `is_measured()` separates the 7 measured stations from the 14 corridor stations, which fall back to a circle at the measured median 1.75 and are labelled as estimates on the drawings. Text carrying the old numbers was rewritten in `proposal.md`, `README.md`, `assumptions.json` (`A-TOD-001`, previously stale in two ways at once — it claimed polygon centroids as origins and called the rings heuristics), `visual/index.html`, and the A0/A3/figure captions. A0 plate `L-01` and A3 page 11 also carried the now-false claim that no walking baseline exists at all.
- Changes (attribution, a compliance gap not a cosmetic one): `sources.json` had 49 entries and no OpenStreetMap entry, although the entire basemap is OSM-derived, and `report/copyright_statement.md` named neither OSM nor Amap. ODbL 1.0 requires attribution, so this was a licence problem in a submission whose own copyright statement asserts no unauthorised third-party assets. Added `OSM-BASEMAP` (`source_type: osm`, ODbL, © OpenStreetMap contributors) and `AMAP-WALKING` to `sources.json`, with the matching `[source:...]` citations in `proposal.md` that the validator requires for every registered source id, plus attribution paragraphs in `report/copyright_statement.md` and `README.md`. The statement records that only the derived reach distances are retained — no Amap tiles, network data or raw API responses are redistributed.
- Changes (reproducibility): `tools/fetch_walk.py` is the measurement script, so `data/walk_isochrones.json` can be regenerated from a clone rather than existing as unexplained numbers. It reads the 7 stations from the same `stations.geojson` the drawings use instead of a hardcoded list, and takes the API key only from `AMAP_KEY` — the key is never written into any file in the repository, which would otherwise put a live credential inside a submission package. It runs 3 workers with 6 backoff retries and reports `_failed_probes` and per-station incomplete bearings, for a specific reason recorded in `tools/README.md`: an earlier 8-worker version lost 3–12 of 17 bearings to transient errors, my `except` turned each into a 0 m vertex, and it printed medians of 0 m that looked like a dramatic finding rather than a bug. That was diagnosed by re-running one failing bearing serially, where all 7 probes succeeded. The committed data has `_failed_probes: 0` and all 7 stations complete.
- Changes (two label defects found by reading the rendered sheets): the 大钟寺 deviation was labelled `偏差 2192m` on A0/A3 while `make_figures.py` said "约 1.7km" — both numbers are real, 2192 m to the polygon centroid and 1732 m to the nearest edge, but neither label said which, so the submission appeared to contradict itself. All three now state both distances and name the measure.
- Verification: `scripts/validate_submission.py --pr-author lumixraku` returned `PASS` over 12 changed files and `scripts/self_check_submission.py --pr-author lumixraku` returned `PASS` with `can_enter_formal_review=YES` and all four sub-reviews green, both with only the pre-existing maintainer-review and provisional-boundary warnings and the three non-blocking `KEY_AREA_PROVISIONAL` notices. Note that the self-check must run under `.venv`; the system interpreter lacks `shapely`/`pyproj`/`jsonschema` and returns a spurious `FAIL` on missing review dependencies. `tools/audit.py` reports 0 off-frame text and 0 label overlaps across all 9 A0 plates and all 11 A3 pages. All 20 sheets were inspected page by page as rendered images, not merely regenerated — that is how both label defects above were found, and one suspected scale-bar defect on `L-01` was disproved by re-reading it at higher zoom (it correctly reads `2km`; the thumbnail had clipped it). `reach_at` was checked numerically against the stored vertices at each label bearing per station. The manifest rehash touched exactly the regenerated files, and the manifest's own entry deliberately carries no `sha256` — a self-hash cannot be correct, and an earlier pass of mine added one before catching it. A repository-wide grep confirms the API key appears in no tracked file.
- Remaining issues: The 14 corridor stations are still circles; they need the same 16-bearing measurement, which is another ~1800 requests. The measured network is Amap's, so it does not account for crossing signal delay, walled breaks, accessibility, or night conditions, and station origins remain OSM provisional points rather than surveyed entrances — the rings will move when real entrances are supplied. A separate Amap POI baseline (not committed) shows 学知园's 5-minute core currently has 0 餐饮 and 0 公交地铁 against a programme that claims breakfast and late-night food, which is a content gap in the station programme, not a drawing error. Amap 3D building views are JS-API browser-rendered and unavailable through the REST service, so no 3D sheet exists yet; Amap publishes no street-view API at all. `tools/` remains fork-only, `matplotlib` is still absent from the maintainer-controlled `requirements-review.txt`, and the `PROV-KEY-003` polygon discrepancy, the `ALLOWED_GEOMETRY_FILES` block on `geometry/stations.geojson` and the `README.md` divergence from upstream are unchanged.

## 2026-08-07 README 展示调整

- Branch: `main`
- Changes: Replaced the inherited repository homepage README with the `lumixraku` Rail Life Rings proposal, including the concept, spatial structure, focus nodes, AI scenarios, phasing, risks, generated figures, PDF links, and formal self-check status.
- Verification: Confirmed all README image and artifact paths resolve within the committed submission package; the previously passing full submission self-check remains the package verification baseline.
- Remaining issues: Official site and key-area boundaries remain provisional as documented above.
